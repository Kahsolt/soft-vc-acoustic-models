{
  "comment": "for slightly model refinement",
  "batch_size": 12,
  "steps": 5000,
  "learning_rate": 1e-4,
  "betas": [0.8, 0.99],
  "weight_decay": 1e-5,
  "log_interval": 10,
  "validation_interval": 250,
  "checkpoint_interval": 500
}